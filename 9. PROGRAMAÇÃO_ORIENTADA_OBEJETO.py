# Objeto é composto de Atributos(dados) e Comportamentos(funções/métodos).
# lista.add(item) a lista passa a ser dona do comportamento add.
# encapsulamento é um dos pilares da O.O

# Class vs. Obj
# Classe funciona como um molde
# Objeto funciona como instâncias criadas através de um molde(class)
